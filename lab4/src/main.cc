#include "../include/menu_array.h"
#include "../include/menu_stack.h"
#include "../include/mydouble_processing.h"
#include "../include/stack_processing.h"

int main() {
  ClearScreen();
  MenuArrayRealization();
  PauseMenu(0);
  // Stack* stack = CreateStack();
  // MenuRealization(stack);
  return 0;
}